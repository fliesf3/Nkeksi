<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.firefly.nkeksi">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="tech.firefly.nkeksi.GettingStartedUser"
            android:screenOrientation="portrait" >


        </activity>
        <activity
            android:name="tech.firefly.nkeksi.Splash"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name="tech.firefly.nkeksi.LoginUser" >

        </activity>
        <activity android:name="tech.firefly.nkeksi.RegisterUser" />
        <activity android:name="tech.firefly.nkeksi.RegisterSecondUser" />
        <activity
            android:name="tech.firefly.nkeksi.Home"
            android:screenOrientation="portrait" />
        <activity
            android:name="tech.firefly.nkeksi.UserProfile"
            android:screenOrientation="portrait" />
        <activity android:name="tech.firefly.nkeksi.TestSearch" />
        <activity android:name="tech.firefly.nkeksi.ForgotPasswordUser" />
        <activity android:name="tech.firefly.nkeksi.RestoDashBoard" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity
            android:name="tech.firefly.nkeksi.MapsActivity">

        </activity>
        <activity android:name=".FoodItemActivity"/>
    </application>

</manifest>